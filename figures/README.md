# Paper figures

Stable publication assets are generated by `scripts/paper_figures/`.

- `fig01_state_landscape.pdf` / `.svg` / `.png`
- `fig02_al_encoding.pdf` / `.svg` / `.png`
- `fig03_state_heterogeneity.pdf` / `.svg` / `.png`
- `fig04_controlled_fidelity.pdf` / `.svg` / `.png`
- `fig05_training_curves.pdf` / `.svg` / `.png`

Temporary review renders and density comparisons are written to
`tmp/figure_reviews/`, not this release directory.

Folders and files containing `v02`, `v03`, or `v04` are internal design history
and are not referenced by the manuscript or intended for public release.
