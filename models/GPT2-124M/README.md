# GPT-2 124M

Download the model configuration and tokenizer files from
[openai-community/gpt2](https://huggingface.co/openai-community/gpt2)
and place them in this directory.

Model weights are not required because the experiment code initializes the
causal language model from configuration. Model payloads are intentionally not
included in the artifact repository.
